Collection --> represent the group of element/object /data into single entity.
Collection framework --> group of Interfaces, classes and algorithms

  Requirement                   |           Best collection usage
1.  Fast search by Index                        ArrayList
2.  Frequent Insert/Delete                      LinkedList
3.  Unique elements                                 Set
4.  Key-value storage                               Map
5.  FIFO processing                                Queue


LinkedList:
    ---> Each element is node
    ---> Each node contains 1. Data
                            2. Address of previous element
                            3. Address of Next element

Queue:
    ---> used to store elements in FIFO(first in first out) order. eg: task scheduler, buffering etc..
Methods in Queue:
    Insert      add()/offer()
    Delete      remove()/poll()
    Examine     element()/peek()

DeQueue:
    ---> also known as 'Deck'(pronounciation) , Double-ended Queue
    --> linear data structure , allows insertion and deletion of elements from both ends(head and tail)
    --> genralization of both FIFO(Queue) and LIFO(stack)

Feature     |       Queue       |       Deque       |       Stack
Insertion         Tail only           Both ends          Top only
Removal           Head only           Both ends          Top only
Access            Head only           Both ends          Top only
Principle          FIFO             Both FIFO & LIFO        LIFO

Characteristics:
1. Bidirectional
2. No capacity restriction(except arrayblockingQueue)
3. No-Index based approach (like Queue,no get(index) method)
4. Null elements are not allowed(exception is linkedlist, preference is not to use)
5. O(1) -- All insertion and deletion at both ends are constant time

DeQue(Interface) ---> Implementation ---> LinkedList (class) , pointer(next,previous), more memory
                          |
                          ---> ArrayDeque (class) --> Resizeable array implementation, no pointers, less memory
Methods in Deque:
    Insert      addFirst(),addLast()/offerFirst(),offerLast()
    Delete      removeFirst(),removeLast()/pollFirst(),pollLast()
    Examine     getFirst(),getLast()/peekFirst(),peekLast()

    Stack       Push/Pop



age = [20,30,23,26]
name = ["Jana","prem","peter","bob"]
city = ["bangalore","chennai"...]

personDetails = ["age:20","name:prem","city:chennai"...]

PriorityQueue:
    -->process bases on the priority instead of insertion order

Queue                           PriorityQueue
FIFO                               Priority Based
Insertion Order                    priority Based
use Linkedlist/ArrList             use Binary Heap(tree based data structure-MinHeap,MaxHeap)
First Inserted Removed             Highest/lowest priority removed


Set:
    --> collection that cannot contains duplicate elements[MUST contains unique elements]
    -->eg: passport number, Aadhar, PAN, Vehicle ID...
    --> models the mathematical set abstraction
Characteristics:
1. No Duplicates
2. No Index access
3. Unordered
4. Allow NULL value
5. Hashcode() and equal()/comparable()

Hashset:
    --> Most common implemenation of set interface
    --> It is backed by HashMap internally

Internal Working:
    --> HashMap created Key:Value pair combination and a dummy object value "PRESENT"(final variable)
    --> Key is the object added via Hashset eg: Apple, Banana etcc
    --> Uses Hashcode() and equal() to determine uniqueness
    --> Elements are stored in hash buckets--insertion order depends on hash code, not Inserstion order
when to use hashset:
1. fast lookup ,insert, delete object
2. union/intersection related mathematical model

Two terms on object creation(parameter)
1. Capacity :- default Capacity of HashSet : 16
2. Load factor :- when to create new hashset space : default :0.75 ~ 75%

Map:
    --> an interface, that stores data in Key-Value pair, where each unique key maps to the exactly one value
    -->Entry:  Each row in map consists of Key-Value pair
    -->underlying DS is "HASHTABLE"
    -->Insertion order is not preserved
    -->Keys should be unique.
    -->Values can be duplicate
    -->Null Key allowed once
    -->Value allows multiple null
    --> Best usecase : fast Searching
HashTable:              HashMap
    -->JDK 1.0             JDK 1.2
    -->Synchronized        Asynchronized
    -->slow                Fast
    -->Safety: High        Safety: considerable
    -->Dont Allow NULL     Allow Null
    -->Def:capacity: 11    Def:capacity: 16
    -->LoadFactor: .75     LoadFactor: .75

Map ----> HashMap
    |---> LinkedHashMap
    |---> TreeMap

HashMap:
    --> Store Key-Value pair
    --> capacity is 16
    --> Key much be unique, value can be duplicated
    --> Key Allows one NULL, value can have multiple NULL
    --> Unordered
    --> Asynchronized
    --> very fast searching



























